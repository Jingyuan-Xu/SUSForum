server:
  port: 8001
spring:
  application:
    name: user
  profiles:
    active: dev
  mvc:
    path-match:
      matching-strategy: ant_path_matcher
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  redis:
    database: 0
    host: 123.249.105.74
    password: sustech-application
    port: 6379

  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: org.postgresql.Driver
      url: jdbc:postgresql://127.0.0.1:5432/sus_forum
      username: postgres
      password: 666666

      #配置连接池大小参数
      initial-size: 15
      min-idle: 10
      max-active: 50

      #配置最长等待时间
      max-wait: 30000

      #连接最小生存时间
      min-evictable-idle-time-millis: 30000
